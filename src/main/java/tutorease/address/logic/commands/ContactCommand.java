package tutorease.address.logic.commands;

/**
 * Represents a contact command with subcommands such as list, add, and others related to managing contacts.
 */
public abstract class ContactCommand extends Command {
    public static final String COMMAND_WORD = "contact";
}
