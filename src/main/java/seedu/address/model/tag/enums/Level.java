package seedu.address.model.tag.enums;

/**
 * Represents the level or intensity of a tag, such as difficulty or workload.
 * This enum is used to classify a company's attributes with three values
 * LOW, MEDIUM, and HIGH.
 */
public enum Level {
    LOW,
    MEDIUM,
    HIGH
}
