package seedu.address.model.person.comparator;

import java.util.Comparator;

import seedu.address.model.person.Person;

/**
 * Represents a comparator class to compare two Person objects based on their specific attribute.
 */
public abstract class PersonCompare implements Comparator<Person> {

}
