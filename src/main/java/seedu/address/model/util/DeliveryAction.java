package seedu.address.model.util;

/**
 * Represents the possible delivery actions that can be performed within the inspect window.
 */
public enum DeliveryAction {
    NONE,
    ADD,
    DELETE,
    ARCHIVE,
    UNARCHIVE
}
