package seedu.address.logic.commands;

/**
 * Abstract find command to find entries in the VendorVault.
 */
public abstract class FindCommand extends Command {
    public static final String COMMAND_WORD = "find";
}
