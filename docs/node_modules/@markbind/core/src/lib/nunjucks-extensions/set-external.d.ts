import { Environment, Extension } from 'nunjucks';
/**
 * Nunjucks extension for sourcing in variables from external sources.
 * Supports only .json and .csv sources for now.
 */
export declare class SetExternalExtension implements Extension {
    private rootPath;
    private env;
    tags: string[];
    constructor(rootPath: string, env: Environment);
    emitLoad(fullPath: string): void;
    parse(parser: any, nodes: any, lexer: any): any;
}
//# sourceMappingURL=set-external.d.ts.map