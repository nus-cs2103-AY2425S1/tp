"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.MarkdownProcessor = void 0;
const markdown_it_1 = __importDefault(require("../lib/markdown-it"));
class MarkdownProcessor {
    constructor(docId) {
        // markdown-it-footnotes state
        this.baseDocId = docId; // encapsulates footnotes in externals (<panel src="...">)
        this.docId = 0; // encapsulates footnotes in <include>s
    }
    renderMd(text) {
        return markdown_it_1.default.render(text, this.docId
            ? { docId: `${this.baseDocId}${this.docId}` }
            : { docId: this.baseDocId });
    }
    renderMdInline(text) {
        return markdown_it_1.default.renderInline(text, this.docId
            ? { docId: `${this.baseDocId}${this.docId}` }
            : { docId: this.baseDocId });
    }
}
exports.MarkdownProcessor = MarkdownProcessor;
//# sourceMappingURL=MarkdownProcessor.js.map